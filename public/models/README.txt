Drop GLB models here to upgrade the scene to photoreal — no code changes needed.

Recognized filenames (auto-loaded if present):
  bike.glb        — replaces CourierBike (zone 1+2)
  rider.glb       — replaces the rider on the bike
  customer.glb    — replaces the Customer at the doorway (zone 5)
  building.glb    — used as a repeating building in zone 2 (recommended < 2k tris)
  car.glb         — used as a repeating car in zone 2
  parcel.glb      — replaces the cardboard box
  phone.glb       — replaces the COD phone
  nepal_map.glb   — replaces the extruded Nepal map shape

Where to find free CC0 / CC-BY models:
  - Quaternius     — https://quaternius.com   (free, low-poly stylized, perfect for this)
  - Poly Haven     — https://polyhaven.com    (CC0, photoreal)
  - Kenney         — https://kenney.nl        (CC0, lots of vehicles + people)
  - Sketchfab      — https://sketchfab.com    (filter by "Downloadable" + license)

Tips:
  - Keep each model under ~5MB to keep the page snappy on mobile.
  - Center the model on the origin and let it have realistic scale (~1m = 1 unit).
  - GLB > GLTF (GLB is a single binary file, faster).
  - For a stylized "AI-illustration" look like the reference: pick Quaternius low-poly.
  - For a fully photoreal look: pick Poly Haven + use the sunset HDRI lighting already configured.
